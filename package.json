{
  "name": "fcc-client-info",
  "version": "1.0.0",
  "description": "A microservice to get client info.",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "ts-node-dev --transpile-only --ignore-watch node_modules src/index.ts",
    "build": "rm -rf ./build && npx tsc",
    "build:start": "npm run build && node build/index.js"
  },
  "engines": {
    "node": "16.x"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "author": "unchainedDavid",
  "repository": {
    "type": "git",
    "url": "https://github.com/unchainedDavid/fcc-client-info.git"
  },
  "keywords": [
    "node",
    "express"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.15",
    "@types/node": "^18.11.18",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.9.4"
  }
}
